#include <iostream>
#include <string>
#include <sstream>
#include <vector>
using namespace std;


int main() {
    // 物品个数n 标签种类m
    int m,n;
    cin >> n >> m;

    vector<int>lables(n);
    vector<int>Ybiaoqian(n);
    vector<int>Nbiaoqian(n);

    return 0;
}
